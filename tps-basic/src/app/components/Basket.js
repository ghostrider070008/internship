import React, { Component } from 'react'

class Basket extends Component {
    render () {
        return (
            <div>
                <button>Корзина</button>
                <p>Если проект до сих пор не загнется и тима не разбежится, то тут будет кнопка при нажатии на которую будет появлятся модальное окно со списком добавленных товаров.</p>
            </div>
        )
    }
}

export default Basket